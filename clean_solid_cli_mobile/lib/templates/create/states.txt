import 'package:equatable/equatable.dart';
import 'package:{{projectName}}/core/error/failures.dart';
import 'package:{{projectName}}/features/{{snakeName}}/domain/entity/{{snakeName}}_entity.dart';
import 'package:{{projectName}}/features/{{snakeName}}/domain/actions/{{snakeName}}_actions.dart';

class {{name}}States extends Equatable {
  final bool isLoading;
  final Failure? error;
  final List<{{name}}Entity>? {{snakeName}}s;
  final {{name}}Entity? currentCriteria;
  final {{name}}Actions? action;
  final String? errorCode;

  const {{name}}States({
    this.isLoading = false,
    this.error,
    this.{{snakeName}}s,
    this.currentCriteria,
    this.action,
    this.errorCode,
  });

  {{name}}States copyWith({
    bool? isLoading,
    List<{{name}}Entity>? {{snakeName}}s,
    {{name}}Entity? currentCriteria,
    {{name}}Actions? action,
    String? errorCode,
    Failure? error,
    bool isClearError = false,
  }) {
    return {{name}}States(
      isLoading: isLoading ?? this.isLoading,
      error: isClearError == true ? null : (error ?? this.error),
      {{snakeName}}s: {{snakeName}}s ?? this.{{snakeName}}s,
      currentCriteria: currentCriteria ?? this.currentCriteria,
      action: action ?? this.action,
      errorCode: errorCode ?? this.errorCode,
    );
  }

  @override
  List<Object?> get props => [
        isLoading,
        error,
        {{snakeName}}s,
        currentCriteria,
        action,
        errorCode,
      ];
}